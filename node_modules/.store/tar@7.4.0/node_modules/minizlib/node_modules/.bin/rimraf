#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/C:/Users/zhengwanlun/Desktop/ROYAL/node_modules/.store/rimraf@5.0.9/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/C:/Users/zhengwanlun/Desktop/ROYAL/node_modules/.store/rimraf@5.0.9/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../rimraf@5.0.9/node_modules/rimraf/dist/esm/bin.mjs" "$@"
else
  exec node  "$basedir/../../../../../rimraf@5.0.9/node_modules/rimraf/dist/esm/bin.mjs" "$@"
fi
